/* Puede reemplazar cualquier función normal */

let sumar = (a, b) => {
    return a + b;
}

// Su similar seria

let sumar = (a, b) => a + b;

console.log(sumar(10, 20));

// tarea en video

let saludar = () => 'Hola Mundo';

// Hay q tener cuidado con los valores cuando son con this.